name: Deploy App

on: workflow_dispatch

jobs:
  run-app:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run application
        run: python3 app.py
